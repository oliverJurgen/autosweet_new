import Router from "next/router";

const SearchResults = (props: any) => {
  const ssrProps = props.ssrProps;
  console.log({ ssrProps });

  return <div>Reworking search result page... IN PROGRESS</div>;
};

export default SearchResults;
